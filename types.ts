type SpotSettings = {
  color: string;
  size: string;
  opacity: number;
  position: {
    x: number;
    y: number;
  };
  blur: string;
};

type GridSettings = {
  enabled: boolean;
  color: string;
  size: string;
  opacity: number;
};

type DotsSettings = {
  enabled: boolean;
  color: string;
  size: string;
  opacity: number;
};
